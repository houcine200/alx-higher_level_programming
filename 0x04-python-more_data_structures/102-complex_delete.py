#!/usr/bin/python3
def complex_delete(my_dict, value):
    targets = []
    for key, val in my_dict.items():
        if key_val is value:
            targets.append(key)
    for x in targets:
        del my_dict[x]
    return (my_dict)
